server:
  port: 8082

spring:
  application:
    name: PAYMENT-SERVICE
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:paymentsdb

eureka:
  client:
    serviceurl:
      defaultZone: http://localhost:9090/eureka
